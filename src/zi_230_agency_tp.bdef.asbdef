interface;
use draft;
extensible;

define behavior for zi_230_agency_tp alias agency
{
  use create;
  use update;
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}